<Window x:Class="BreakoutWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Width="800" Height="650">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center" >
                <TextBlock Text="Pont: " FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="ScoreText" Text="0" Margin="4,0" VerticalAlignment="Center"/>
                <TextBlock Text="  Élet: " FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <TextBlock x:Name="LivesText" Text="3" Margin="4,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="StartButton" Click="StartButton_Click" Margin="4">Start</Button>
                <Button x:Name="RestartButton" Click="RestartButton_Click" Margin="4">Újraindítás</Button>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Margin="8" BorderBrush="DarkGray" BorderThickness="1" Background="Black">
            <Canvas x:Name="GameCanvas" Background="Black" Focusable="True" MouseMove="GameCanvas_MouseMove"/>
        </Border>
    </Grid>
</Window>